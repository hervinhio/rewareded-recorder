import { createSlice } from "@reduxjs/toolkit";

export class Version {
    static slice = createSlice({
        name: 'Version',
        initialState: 2,
        reducers: {
            
        }
    })
}
